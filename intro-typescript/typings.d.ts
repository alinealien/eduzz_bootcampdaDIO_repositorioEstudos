/* o que for adcionado aqui, se tiver o mesmo nome dos tipos definidos da biblioteca 
externa que utilizamos, ele irá somar as duas interfaces e ao fim teremos acesso aos seus itens*/

interface JQuery {
    novaFuncao(): void;
}